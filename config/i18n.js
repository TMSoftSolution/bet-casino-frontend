import en from '../locales/en.json'
import es from '../locales/es.json'
import { numberFormats } from './numberFormats'

export default {
  locale: 'en',
  fallbackLocale: 'en',
  numberFormats,
  messages: { en, es }
}
